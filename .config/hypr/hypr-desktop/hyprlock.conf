general {
  immediate_render = true
}

background {
  monitor = 
  path = $HOME/Pictures/Wallpapers/green_leaves.jpg

  blur_passes = 2
  blur_size = 2
}

input-field {
  monitor = DP-1
  fade_on_empty = false
  size = 15%, 4%
  position = 0, -150
  rounding = 0

  inner_color = rgba(21, 35, 38, 1.0)
  outer_color = rgba(149, 194, 161, 1.0)
  font_color = rgba(149, 194, 161, 1.0)
  outline_thickness = 2

  font_family = JetBrainsMono Nerd Font Bold

  placeholder_text = <i>Sign in</i>
  font_size = 50
  hide_input = false

  fail_color = rgba(255, 0, 123, 1.0)
  fail_text = <b>Wrong attempts: $ATTEMPTS</b>
  fail_transition = 300
}

label {
  monitor = DP-1
  text = cmd[update:1000] echo "$(date "+%A, %B, %d")"
  font_size = 20
  font_family = JetBrainsMono Nerd Font
  color = rgba(149, 194, 161, 1.0)

  position = 0, 180
  halign = center
  valign = center
}

label {
  monitor = DP-1
  text = cmd[update:1000] echo "$(date "+%I:%M")"
  font_size = 110
  font_family = JetBrainsMono Nerd Font Bold
  color = rgba(149, 194, 161, 1.0)

  position = 0, 50
  halign = center
  valign = center
}

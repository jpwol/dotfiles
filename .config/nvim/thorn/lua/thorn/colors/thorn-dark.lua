local colors = require("thorn.colors.colors")

return {
	comment = colors.green,
	statement = colors.orange,
	identifier = colors.lightblue,
	special = colors.lightblue,
	delimiter = colors.gray,
	constant = colors.yellow,
	str = colors.lightgreen,
	func = colors.pink,
	types = colors.cyan,
	operator = colors.red,
	var = colors.white,
	param = colors.yellow,
	icon = colors.lightblue,

	cursorline = colors.cursorline,
	separator = colors.separator,
}
